# LangGraph and LangChain
langgraph>=0.2.0
langchain>=0.3.0
langchain-ollama>=0.2.0
langchain-community>=0.3.0

# Vector store and embeddings
faiss-gpu
sentence-transformers>=2.0.0

# FastAPI backend
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# Document processing
pypdf>=4.0.0
python-docx>=1.1.0
pdfplumber>=0.11.0
poppler-utils

# OCR for images and scanned PDFs
pytesseract>=0.3.10
Pillow>=10.0.0
pdf2image>=1.16.3

# Web scraping for lawyer search
httpx>=0.27.0
beautifulsoup4>=4.12.0
aiohttp>=3.9.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Async support
aiofiles>=24.1.0

# Caching for performance
cachetools>=5.5.0

# In-process job scheduler (hearing reminders, cause-list polling, notifications)
APScheduler>=3.10,<4.0

# S3-compatible object storage client (Legal Document Vault -> Cloudflare R2)
boto3>=1.34.0

# Database (PostgreSQL)
sqlmodel>=0.0.22
psycopg[binary]>=3.2
pgvector>=0.3.6

# Auth
PyJWT>=2.8.0
bcrypt>=4.1.0

# Payments (Braintree sandbox)
braintree>=4.28.0

# Hybrid retrieval (BM25 sparse index; reranker comes from sentence-transformers)
rank-bm25>=0.2.2

flake8
